# SmolVLA Code Flow Diagram

## 🔄 Training Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. CONFIGURATION                                                │
│    File: configuration_smolvla.py                               │
│    - Learning rate, batch size, architecture params             │
│    - Input/output dimensions                                    │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. DATA LOADING                                                 │
│    File: datasets/lerobot_dataset.py                            │
│    - Load images, states, actions                               │
│    - Apply delta_timestamps                                     │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. PREPROCESSING                                                │
│    File: processor_smolvla.py                                   │
│    Steps:                                                       │
│    - Rename observations                                        │
│    - Add batch dimension                                        │
│    - Add newline to task                                        │
│    - Tokenize language                                          │
│    - Move to device                                             │
│    - Normalize states/actions                                   │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. MODEL FORWARD PASS                                           │
│    File: modeling_smolvla.py                                    │
│                                                                 │
│    ┌─────────────────────────────────────────────┐              │
│    │ SmolVLAPolicy.forward()                     │              │
│    │   └─> VLAFlowMatching.forward()             │              │
│    └─────────────────────────────────────────────┘              │
│                       │                                         │
│                       ▼                                         │
│    ┌─────────────────────────────────────────────┐              │
│    │ SmolVLMWithExpertModel.forward()            │              │
│    │   ├─> embed_image()                         │              │
│    │   ├─> embed_language_tokens()               │              │
│    │   └─> forward_attn_layer() or               │              │
│    │       forward_cross_attn_layer()            │              │
│    └─────────────────────────────────────────────┘              │
│                       │                                         │
│                       ▼                                         │
│    ┌─────────────────────────────────────────────┐              │
│    │ Flow Matching                               │              │
│    │   - Predict action sequence                 │              │
│    └─────────────────────────────────────────────┘              │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. LOSS COMPUTATION                                             │
│    File: modeling_smolvla.py                                    │
│    - Compute flow matching loss                                 │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. BACKWARD PASS                                                │
│    File: scripts/lerobot_train.py                               │
│    - loss.backward()                                            │
│    - optimizer.step()                                           │
│    - scheduler.step()                                           │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│ 7. POSTPROCESSING                                               │
│    File: processor_smolvla.py                                   │
│    - Unnormalize actions                                        │
│    - Move to CPU                                                │
└─────────────────────────────────────────────────────────────────┘
```

## 🏗️ Architecture Components

```
┌─────────────────────────────────────────────────────────────────┐
│                    SmolVLA Architecture                         │
└─────────────────────────────────────────────────────────────────┘

Inputs:
  ├─ Images (cameras)
  ├─ State (robot state)
  └─ Language (task description)

        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│ Vision Encoder                                                  │
│ (Frozen by default)                                             │
│   └─> Image embeddings                                          │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│ VLM (Vision-Language Model)                                     │
│ - Processes vision + language                                   │
│ - num_vlm_layers: 16 (configurable)                             │
│ - Generates key-value cache                                     │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       │ (KV Cache)
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│ Action Expert                                                   │
│ - Smaller than VLM (expert_width_multiplier: 0.75)              │
│ - num_expert_layers: -1 (same as VLM)                           │
│ - Cross-attention to VLM                                        │
│ - Processes action tokens                                       │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│ Projection Layers                                               │
│ - state_proj: State → VLM hidden size                           │
│ - action_in_proj: Action → Expert hidden size                   │
│ - action_out_proj: Expert hidden size → Action                  │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
                  Actions (chunk_size: 50)
```

## 📁 File Organization

```
src/lerobot/policies/smolvla/
│
├── configuration_smolvla.py
│   └── SmolVLAConfig
│       ├── Training params (lr, batch, scheduler)
│       ├── Architecture params (layers, attention)
│       └── Input/output params (dims, image size)
│
├── modeling_smolvla.py
│   ├── SmolVLAPolicy (wrapper)
│   │   ├── _get_action_chunk()  ← Action generation
│   │   └── forward()             ← Training forward
│   │
│   └── VLAFlowMatching (core model)
│       ├── __init__()            ← Initialize components
│       └── forward()             ← Main forward pass
│
├── smolvlm_with_expert.py
│   └── SmolVLMWithExpertModel
│       ├── embed_image()         ← Vision encoding
│       ├── embed_language_tokens() ← Language encoding
│       ├── forward_attn_layer()   ← Self-attention
│       ├── forward_cross_attn_layer() ← Cross-attention
│       └── forward()              ← Architecture forward
│
└── processor_smolvla.py
    ├── make_smolvla_pre_post_processors() ← Setup pipeline
    └── SmolVLANewLineProcessor    ← Task preprocessing
```

## 🔑 Key Modification Points

### 1. Hyperparameters
```
configuration_smolvla.py
├── optimizer_lr: float = 1e-4          ← Learning rate
├── scheduler_warmup_steps: int = 1_000  ← Warmup
└── scheduler_decay_steps: int = 30_000  ← Decay schedule

configs/train.py
└── batch_size: int = 8                  ← Batch size
```

### 2. Architecture
```
configuration_smolvla.py
├── num_vlm_layers: int = 16             ← VLM depth
├── num_expert_layers: int = -1          ← Expert depth
├── expert_width_multiplier: float = 0.75 ← Expert size
├── attention_mode: str = "cross_attn"    ← Attention type
└── vlm_model_name: str = "..."          ← VLM backbone

smolvlm_with_expert.py
└── SmolVLMWithExpertModel.__init__()    ← Architecture setup
```

### 3. Input/Output
```
configuration_smolvla.py
├── max_state_dim: int = 32              ← State dimension
├── max_action_dim: int = 32             ← Action dimension
├── resize_imgs_with_padding: (512, 512) ← Image size
└── chunk_size: int = 50                 ← Action chunk

processor_smolvla.py
└── make_smolvla_pre_post_processors()   ← Preprocessing
```

### 4. Forward Pass Logic
```
modeling_smolvla.py
└── VLAFlowMatching.forward()            ← Main logic

smolvlm_with_expert.py
├── forward_attn_layer()                  ← Self-attention
└── forward_cross_attn_layer()           ← Cross-attention
```

## 🎯 Quick Modification Checklist

- [ ] Learning rate: `configuration_smolvla.py` line 76
- [ ] Batch size: `configs/train.py` line 55
- [ ] Architecture layers: `configuration_smolvla.py` lines 97-100
- [ ] Input dimensions: `configuration_smolvla.py` lines 43-44
- [ ] Image size: `configuration_smolvla.py` line 47
- [ ] Attention mode: `configuration_smolvla.py` line 91
- [ ] VLM model: `configuration_smolvla.py` line 86
- [ ] Preprocessing: `processor_smolvla.py` lines 69-85
- [ ] Forward pass: `modeling_smolvla.py` line 448
- [ ] Architecture: `smolvlm_with_expert.py` line 404

